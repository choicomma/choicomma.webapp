# Git Push Policy Rule

- Do NOT automatically run `git commit` or `git push` after code edits or bug fixes.
- Only modify files locally and test/verify changes locally.
- Only perform `git commit` and `git push` when the user explicitly requests to push (e.g., "푸시해줘", "push").
